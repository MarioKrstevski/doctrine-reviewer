# Railway waits for the health check to pass before routing traffic to a
# new container, so deploys do not serve 502s during the rollover.
[deploy]
healthcheckPath = "/where"
healthcheckTimeout = 120
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
