### Suggest button position

The **✎ Suggest an edit** button floats over the reviewer. If it overlaps
another add-on's button, nudge it here.

- `button_top_offset` — pixels from the top of the reviewer window.
- `button_right_offset` — pixels from the right edge.

Restart Anki after changing these.
